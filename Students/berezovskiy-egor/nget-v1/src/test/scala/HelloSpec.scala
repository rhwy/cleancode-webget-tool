import org.scalatest._
import fr.egor.ArgsParser

class ArgsParserTest extends FlatSpec with Matchers {
  "Hello" should "have tests" in {
    val ap = new ArgsParser
    val expected =
    true should === (true)
  }
}
